import React from 'react';
import './Navpills.css';

const Navpills = props => {

    <div>
        <ul className="nav nav-pills nav-justified">
            <li><a href="/">Cat Clicker</a></li>
            <li className={props.message.indexOf('correct') !== -1 ?
                // Passing something through to make sure it's correct
            }

        // Keep track of score and pass through to user

export default Navpills;