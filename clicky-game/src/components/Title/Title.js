import React from "react";
import "./Title.css";

const Title = props => (
    <div className="titlebox">
        <h1>Kitty Clicky</h1>

        <p>Click on each image that is a cat. Try to set a high score!</p>
    </div>
);
export default Title;
